<Window x:Class="SCADA.WindowArchiveTable"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Архив" Width="800" Height="600" ShowInTaskbar="False" WindowStartupLocation="CenterScreen" FontSize="16" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" ResizeMode="CanResizeWithGrip">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ToolBar>
            <Button Click="Button_Click" ToolTip="Печать">
                <Image Source="Images/print_printer_5893.png">
                </Image>
            </Button>
        </ToolBar>       
        <DataGrid Grid.Row="1" x:Name="DGTable">          
        </DataGrid>
    </Grid>
</Window>
